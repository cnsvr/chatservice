package com.example.chatservice.enums;

public enum MessageCode {
    CHAT_MESSAGE, // All messages sent by users.
    USER_JOINED, // A user joined the chat room.
    USER_LEFT // A user left the chat room.
}
